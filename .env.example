# Without a key, only live Everland official pages are supported (no fixtures).
SEARCH_PROVIDER=everland
# Set SEARCH_PROVIDER=openai to enable natural language + multi-provider web search.
OPENAI_API_KEY=
OPENAI_MODEL=gpt-5-mini
GEMINI_API_KEY=
GEMINI_MODEL=gemini-2.5-flash
SEARCH_CONCURRENCY=3
